import XCTest
@testable import ObservablePackage

final class ObservablePackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ObservablePackage().text, "Hello, World!")
    }
}
